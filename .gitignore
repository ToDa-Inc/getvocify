# Environment (contains secrets - never commit)
.env
.env.local
backend/.env

# Python
__pycache__/
*.py[cod]
*.pyo
.Python
.venv/
venv/
.venv-test/

# Node
node_modules
dist
dist-ssr
*.local
bun.lockb

# Logs and local runtime
logs/
*.log

# Point-in-time schema snapshot from backend/scripts/dump_schema.py - a
# diagnostic artifact, never a source of truth (git is). Regenerate on demand.
backend/migrations/_schema_dump.sql

# GTM / workspace data (lives in ../vocify-workspace)
*.csv
*.xlsx
tmp_*/
.codex_tmp/
.memsearch/

# Build artifacts
chrome-extension.zip
vite.config.ts.timestamp-*

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
