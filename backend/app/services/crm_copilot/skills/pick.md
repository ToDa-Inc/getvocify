# pick

Several matches → offer_user_choices with pick:contact:{id} or pick:deal:{id} and short labels (name + company).
After they pick, get_contact / get_deal (full record: notes, fields, deals) and continue the original ask. Do not restart as a blank extract.
