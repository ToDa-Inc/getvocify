# voice

Voice note or a long sales dump: extract_sales_update with the transcript, then preview_write (skip_deal true unless they named a deal), then apply_write.
Do not create a second memo if one is already in session (memo_id in context).
